# State: storybook-ci-deploy

**Current Phase:** archive (validation failed)
**Status:** blocked

## Validation Summary
The remote validation plan could not be executed because the current local repository does not have any commits and is not connected to a remote GitHub repository.

## Pending Verification
- PR run triggers build job and skips deploy
- Main branch run triggers deploy path
- Fail-fast behavior blocks deploy when a gate fails